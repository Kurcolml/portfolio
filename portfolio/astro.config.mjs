// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  base: '/portfolio/',  // Reemplaza con el nombre de tu repo
  output: 'static',          // Para que genere archivos estáticos
});
